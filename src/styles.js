import {
  StyleSheet
} from 'react-native'

// app level generic styles
export const genericStyles = StyleSheet.create({
  centeredContent: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  }
})